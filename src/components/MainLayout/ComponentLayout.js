import React from 'react'
import { Menu, BackTop, Layout } from 'antd'
import MainLayout from './MainLayout'
import styles from './MainLayout.css'

const ComponentLayout = ({ location, children }) {
  return (
    <MainLayout>
      
    </MainLayout>
  )
}